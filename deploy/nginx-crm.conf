# Nginx at c:\nginx — put this in c:\nginx\conf\conf.d\ or include from http {}
# Upstream for CRM dashboard
upstream crm_dashboard {
    server 127.0.0.1:8000;
}

# Server block (adjust server_name and paths as needed)
server {
    listen 80;
    server_name your-domain-or-ip;

    location /static/ {
        alias C:/serve/H.F-Capital-CRM/staticfiles/;
    }

    location / {
        proxy_pass http://crm_dashboard;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
